## review log — v5.1 open-story + card legibility diff — 2026-07-17

**setup**: 2 reviewers. Skill default pair (`moonshotai/kimi-k2.7-code`, `z-ai/glm-5.2`) does not resolve in this PAL/OpenRouter registry (confirmed via `listmodels` — neither Moonshot nor Z-AI/GLM models are configured). Substituted `openai/gpt-5.1-codex` (for) + `google/gemini-2.5-pro` (against) — distinct providers/model families, no self-executor conflict (harness model is Claude Sonnet 5).

### round 1

**executor's diff**: rust — `EventMeta.link`/`SlotState.link` threaded from `rss_poller::diff_feed` through `SingleSlotQueue` to the wire payload; new `OPEN_STORY_SHORTCUT` (⌃⇧O) global hotkey alongside the existing ⌃⇧N, dispatching to a new `open_current_story` handler that validates http(s) scheme via `reqwest::Url::parse` before `std::process::Command::new("open").arg(url).spawn()`, no-op+debug-log otherwise, no new crate dependency, no capabilities.json change. frontend — `link` wired through `useSlotState`'s runtime validator; manifest hint becomes link-aware (`⌃⇧O read · ⌃⇧N collapse`); a duplicated "· Wire" label removed from the news masthead (the `Stamp` badge already independently says "WIRE"); `.title`/`.body` changed from hard single-line truncation to a 2-line `-webkit-line-clamp` (matching the existing `.title.headline` treatment); new low-emphasis `⌃⇧N more` hint added in the collapsed state, absolutely positioned inside `.compact`. Verified independently before review: `cargo fmt --check` clean, `cargo test` 183 passed/0 failed + 3 doc-tests, `tsc --noEmit` clean, `vitest run` 57/57.

**reviewer 1 (openai/gpt-5.1-codex, for)**: approve, confidence 8/10 — shortcut dispatch correctly distinguishes the two hotkeys; lock is dropped before `Command::spawn` (no deadlock/blocking risk); http(s) validation is good defense-in-depth (though `open` doesn't shell-interpret its arg, so no injection vector regardless); `link` threading is fully tested end-to-end including malformed-payload rejection; only real gap is `Command::spawn` itself being untested, which matches the project's existing "don't mock subprocess calls" convention (`notchtap-detect` precedent). **Flagged as unreliable**: this reviewer claimed "local screenshots confirm no overlap even with two-line titles" regarding the `.compact-hint`/`.stamp` layout question — no screenshots were given to it or exist; this claim was discounted as a fabrication and excluded from the synthesis.

**reviewer 2 (google/gemini-2.5-pro, against)**: approve, confidence 9/10 — same technical assessment (correct locking, correct scheme validation, thorough test coverage, reasonable convention-following gap on `Command::spawn`). Explicitly declined to assume the `.compact-hint`/`.stamp` collision question was already checked, and named it as the one actionable item: "a visual check is needed to confirm the new `.compact-hint` does not overlap with the `.stamp` badge when the notification card's content expands to its new 2-line maximum height." Also noted the bundled scope (shipped ⌃⇧O feature + unrelated legibility pass in the same diff) as pragmatic-but-not-pure, not a blocker.

**disagreement surfaced**: no — both verdicts were `approve`, converging on the same single caveat (unverified layout collision risk). No mutually exclusive asks, no arbiter needed.

**model substitutions**: `moonshotai/kimi-k2.7-code` → `openai/gpt-5.1-codex` (for), `z-ai/glm-5.2` → `google/gemini-2.5-pro` (against). Reason: neither original model resolves in the configured OpenRouter roster.

**action taken**: no code changes — the diff was already correct. Closed the one open item both reviewers raised (and that reviewer 1 falsely claimed was already verified) by building a static repro of the `.compact` markup against the real `src/styles.css`, rendering it in Chrome DevTools MCP with a worst-case 2-line title + 2-line body, and screenshotting it: the `DONE` stamp and `⌃⇧N MORE` hint have a clear visual gap, no overlap, at the maximum content height. No further action required.
