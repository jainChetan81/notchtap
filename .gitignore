# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# swift package build output
notchtap-detect/.build

# local AI-tool state and credentials
.claude/settings.local.json
.claude/worktrees/
.mcp.json
opencode.json
opencode.json.bak*
.opencode/

# Cross-tool skill discovery. The tracked root `skills` symlink points to
# the canonical `.claude/skills` tree; nested copies and other state stay local.
**/.agents/
!/.agents/
/.agents/*
!/.agents/skills

# throwaway exploratory captures (live-API research, screenshots, etc.)
research/

# plan 104: local cmake build output for the vendored mediaremote-adapter
# (never committed — the install target is /usr/local/lib, not this tree)
src-tauri/vendor/mediaremote-adapter/build/
